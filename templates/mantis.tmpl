mantis.blockchains.testnet-internal-nomad.bootstrap-nodes = [
  {{ range service "mantis-kevm2-mantis-miner-server" -}}
    "enode://  {{- with secret (printf "kv/data/nomad-cluster/mantis-kevm2/%s/enode-hash" .ServiceMeta.Name) -}}
      {{- .Data.data.value -}}
      {{- end -}}@{{ .ServiceMeta.Name }}.portal.dev.cardano.org:{{ .ServiceMeta.IngressPort }}",
  {{ end -}}
]
